export const metadata = {
  title: 'Engineer Dashboard — Synapse Nexus AI',
}

export default function EngineerDashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
